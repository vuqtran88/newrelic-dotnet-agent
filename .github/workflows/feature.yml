name: test feature

# Does not run on PUSH since we have already ran all the test
on:
  pull_request:
    branches: [ main ]
  release:
    types: [ published ]
  workflow_dispatch:

env:
  scripts_path: ${{ github.workspace }}\build\scripts
  tools_path: ${{ github.workspace }}\build\Tools
  DOTNET_NOLOGO: true

jobs:
  
  set_agent_validation:
    runs-on: windows-2019
    steps:
    - name: set agent version
      run: |
        echo "agent_version=hello" >> $GITHUB_ENV
      shell: powershell
    - name: get agent version within a step
      run: |
        echo $agent_version
      shell: powershell
    
    - name: Set env
      run: echo "GITHUB_SHA_SHORT=$(echo $GITHUB_SHA | cut -c 1-6)" >> $GITHUB_ENV
    - name: Test
      run: echo $GITHUB_SHA_SHORT
      
  get_agent_validation:
    runs-on: windows-2019
    steps:
    - name: get agent version
      run: |
        echo $agent_version
      shell: powershell

      
